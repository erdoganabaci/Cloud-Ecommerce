generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://admin:admin@10.105.135.76:5432/postgres?schema=public"

  
}

model Product {
  id    Int     @id @default(autoincrement())
  name String?
  totalCount Int?
  category String?
  purchasedAt DateTime @default(now())
}

